# Game-of-function-1
def summ(num):                # defining the function
    sum = 0
    for i in num:              # adding the given inputs 
        sum+=i 
    return sum
num = (8,2,3,0,7)           # input
print(summ(num))
